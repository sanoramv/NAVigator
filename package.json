{
  "name": "mutual-lens",
  "version": "0.1.0",
  "description": "Indian Mutual Fund Knowledge Base — local-first browser app",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "idb": "^8"
  },
  "devDependencies": {
    "vite": "^5",
    "gh-pages": "^6"
  }
}
